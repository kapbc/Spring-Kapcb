<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/rabbit
    http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd
    http://www.springframework.org/schema/context
    https://www.springframework.org/schema/context/spring-context.xsd">
    <description>Spring-RabbitMQ</description>

    <context:property-placeholder location="properties/rabbitmq-config.properties"/>

    <!-- 连接配置 -->
    <rabbit:connection-factory id="connectFactory" host="${rabbitmq.host}" username="${rabbitmq.username}"
                               password="${rabbitmq.password}" port="${rabbitmq.port}"
                               virtual-host="${rabbitmq.virtual.host}"/>

    <rabbit:admin connection-factory="connectFactory"/>

    <!-- spring template声明-->
    <rabbit:template exchange="amqpExchange" id="amqpTemplate" connection-factory="connectFactory"
                     message-converter="jsonMessageConverter"/>

    <!-- 消息对象json转换类 -->
    <bean id="jsonMessageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"/>

    <!-- 申明一个消息队列Queue -->
    <!--
        durable:是否持久化
        auto_delete: 当所有消费客户端连接断开后，是否自动删除队列
        exclusive: 仅创建者可以使用的私有队列，断开后自动删除
    -->
    <rabbit:queue id="test_queue_key" name="test_queue_key" durable="true" auto-delete="false" exclusive="false"/>

    <!-- 交换机定义 -->
    <!--
        rabbit:direct-exchange：定义exchange模式为direct，意思就是消息与一个特定的路由键完全匹配，才会转发。
        rabbit:binding：设置消息queue匹配的key
    -->
    <rabbit:direct-exchange id="test_mq_exchange" name="test_mq_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <rabbit:binding queue="test_queue_key" key="test_queue_key"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <!-- 监听配置 -->
    <rabbit:listener-container connection-factory="connectFactory" acknowledge="auto">
        <rabbit:listener ref="rabbitQueueListener" queues="test_queue_key"/>
    </rabbit:listener-container>
</beans>
